function removeKeys() {
    var obj = { a: 1, b: [], c: '' }; 
    var arr = ['a', 'c'];
        if (arr[i] in obj){
        Object.keys(obj).forEach (  el => {
            } )
        }else{
            res.push(obj)
        } 
    }
    var res = {};
    
removeKeys()

/*
function clearNumbers(arr){
    var arr = [[[1, "a", {}], [{}, 2, 3], [null, -3, 'q']]];
    function isNumeric(arr) {
        return !isNaN(parseFloat(arr)) && isFinite(arr);
      
    delete arr[i<0]
      console.log(arr)
}*/